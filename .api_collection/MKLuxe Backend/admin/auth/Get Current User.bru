meta {
  name: Get Current User
  type: http
  seq: 2
}

get {
  url: {{BACKEND_URI}}/api/v1/auth/me
  body: none
  auth: inherit
}

headers {
  ~Authorization: Bearer {{ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
